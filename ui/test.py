
# root = tk.Tk()
# root.title('抖音作品下载器')
# root.geometry('600x300')
# label1 = tk.Label(root, text='主页地址:')
# entry1 = tk.Entry(root)  # 输入框
# btn1 = tk.Button(root, text='按钮1', bg='red')
# btn2 = tk.Button(root, text='按钮2', bg='red')
# btn1 = tk.Button(root, text='检查', bg='red')
# label1.grid(row=0, column=0)
# entry1.grid(row=0, column=1)
# btn1.grid(row=0, column=2)
#
# label2 = tk.Label(root, text='选择保存地址：')
# entry2 = tk.Entry(root, state='disabled')  # 输入框
# btn1 = tk.Button(root, text='打开', bg='red', command=open_dir)
#
# label2.grid(row=1, column=0)
# entry2.grid(row=1, column=1)

# btn4 = tk.Button(root, text='按钮4', bg='red')
# btn5 = tk.Button(root, text='按钮5', bg='red')
# btn6 = tk.Button(root, text='按钮6', bg='red')
# btn4.grid(row=1, column=0)
# btn5.grid(row=1, column=1)
# btn6.grid(row=1, column=2)
# root.mainloop()

